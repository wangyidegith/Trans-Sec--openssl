零 数学概念
（零）数学原理：椭圆曲线数学。


（一）公私钥对的生成
0 选择一个椭圆曲线，并选择相关参数
（0）椭圆曲线：y^2 = x^3 + ax + b
（1）生成点P：该点是椭圆曲线上的一个特定点。
（2）阶n：生成点P的阶，即nP = 无穷大

1 生成私钥d：1 <= d <= n

2 公钥Q = dP


（二）ECDSA：详见图片《ECDSA》，其实也没看懂，但是至少知道了为什么ECDSA生成的签名值需要用两个部分一个r一个s存储，其中r是iv，s是签名值。
